version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENV=production
    volumes:
      - ./backend/sample_data.csv:/app/sample_data.csv:ro
    restart: unless-stopped